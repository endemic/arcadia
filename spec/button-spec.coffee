describe 'Arcadia.Button', ->

  beforeEach ->
    @label = new Arcadia.Button 0, 0, 'Click here!'

  afterEach ->
    @label = null

  xit 'can draw itself', ->
    console.log 'pass'

  xit 'can get its width', ->
    console.log 'pass'
